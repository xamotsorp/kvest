# Эта папка больше не используется

Фото для «Собери момент» и для карточки «Какая ты нейросеть» теперь берётся из
того, что человек сам загружает на экране регистрации (после PIN) — see
`backend/src/routes/register.js`. Файлы сохраняются в `media/uploads/`.

Эта папка (`media/photos/`) — наследие первой версии, можно удалить.
