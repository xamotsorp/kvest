PIN=1234
ADMIN_PASSWORD=change-me
SESSION_SECRET=change-me-too
DATABASE_URL=postgres://app:app@db:5432/stations
PORT=3000
# set to 1 only when served over HTTPS (production) — cookies won't work over plain HTTP if set
COOKIE_SECURE=0
